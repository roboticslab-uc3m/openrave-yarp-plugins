# Generated by Juan G Victores 2013
# Thanks Rosen Diankov for all OpenRAVE developments

option(ENABLE_OpenraveYarpCoupled "Enable/disable OpenraveYarpCoupled" ON)

if(ENABLE_OpenraveYarpCoupled)

include_directories(${OpenRAVE_INCLUDE_DIRS})

link_directories(${OpenRAVE_LIBRARY_DIRS})

add_library(OpenraveYarpCoupled MODULE OpenraveYarpCoupled.cpp)

set_target_properties(OpenraveYarpCoupled PROPERTIES COMPILE_OPTIONS "${OpenRAVE_CXX_FLAGS}"
                                                     LINK_FLAGS "${OpenRAVE_LINK_FLAGS}")

target_link_libraries(OpenraveYarpCoupled YARP::YARP_OS
                                          YARP::YARP_init
                                          YARP::YARP_dev
                                          YARP::YARP_sig
                                          ${OpenRAVE_LIBRARIES}
                                          Boost::boost)

include(InstallOpenravePlugin)

install_openrave_plugin(OpenraveYarpCoupled)

endif()
