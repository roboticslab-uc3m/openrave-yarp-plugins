# Generated by Raul de Santos Rico 2017
# Thanks Rosen Diankov for all OpenRAVE developments

option(ENABLE_OpenraveYarpWorldRpcResponder "Enable/disable OpenraveYarpWorldRpcResponder" ON)

if(ENABLE_OpenraveYarpWorldRpcResponder)

include_directories(${CMAKE_CURRENT_SOURCE_DIR}
                    ${OpenRAVE_INCLUDE_DIRS}
                    ${YARP_INCLUDE_DIRS}
                    ${Boost_INCLUDE_DIRS})

link_directories(${OpenRAVE_LIBRARY_DIRS}
                 ${Boost_LIBRARY_DIRS})

add_library(OpenraveYarpWorldRpcResponder OpenraveYarpWorldRpcResponder.cpp)

set_target_properties(OpenraveYarpWorldRpcResponder PROPERTIES COMPILE_FLAGS "${OpenRAVE_CXX_FLAGS}"
                                                               LINK_FLAGS "${OpenRAVE_LINK_FLAGS}")

target_link_libraries(OpenraveYarpWorldRpcResponder ${YARP_LIBRARIES}
                                                    ${OpenRAVE_LIBRARIES})

include(InstallOpenravePlugin)

install_openrave_plugin(OpenraveYarpWorldRpcResponder)

endif()
